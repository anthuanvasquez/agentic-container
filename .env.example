# GitHub authentication is handled interactively via `gh auth login`.
# No required environment variables at build time.

# Optional: Context7 API key if you add MCP servers later
# CONTEXT7_API_KEY=
